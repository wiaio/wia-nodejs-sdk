'use strict';

var deviceOptions =  {
    token: process.env.DEVICE_TOKEN
};

describe('Device', function () {
    before(function (done) {
        done();
    });

    after(function (done) {
        done();
    });
});
